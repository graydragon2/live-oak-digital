# No Sitemap line and no domain-specific rules — Rachel doesn't have her own
# domain yet, so there's nothing to point search engines at that's worth
# indexing as canonical. Add a "Sitemap: https://<realdomain>/sitemap.xml"
# line here once she registers one (and restore sitemap.xml — see the note
# left in that file).

User-agent: *
Allow: /
