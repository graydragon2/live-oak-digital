# Points at the interim Cloudflare Workers URL — Rachel doesn't have her
# own domain yet. Once she registers one, update this, sitemap.xml, every
# page's canonical/og:url/JSON-LD url, and CONFIG.seo.siteUrl in config.js
# (this file can't pull from config.js automatically — search engines read
# it directly).

User-agent: *
Allow: /

Sitemap: https://cookieandcrumb.graydragon2.workers.dev/sitemap.xml
