<?xml version="1.0" encoding="UTF-8"?>
<!--
  Emptied out — Rachel doesn't have her own domain yet, so there are no
  URLs worth publishing here (the interim Cloudflare Workers address is
  deliberately not being pointed at as canonical; see config.js's
  CONFIG.seo.siteUrl and robots.txt for the same note). Once she registers
  a real domain, refill this with one <url><loc> entry per page:
  index.html, services.html, about.html, reviews.html, contact.html —
  each as https://<realdomain>/<page>.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
</urlset>
